Useful commands:

------------------
Activate venv:

# Local:
.\.venv\Scripts\Activate.ps1  

# Cluster:
source .venv/bin/activate


Deactivate venv:
deactivate
------------------

------------------
Run one simulation:
julia --project=. SI_simulate_unif_grid.jl